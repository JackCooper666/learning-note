## Problem  q_array is a list, and the bias_array is a numpy array, will the data type mismatch occuer in the quaternion_multiply()

`quaternion_multiply()` will **work fine** with both inputs as:

- Python lists
    
- NumPy arrays
    
- Or a mix (list + array)
- ----------------------------------------------------------------------

